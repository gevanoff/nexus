services:
  vllm:
    container_name: nexus-vllm-dev
    ports:
      - "${DEV_VLLM_PORT:-18000}:8000"
    volumes:
      - ${HF_HOME_BIND_SOURCE:-/var/lib/huggingface}:${HF_HOME:-/var/lib/huggingface}

networks:
  nexus:
    name: nexus-dev

volumes: {}
