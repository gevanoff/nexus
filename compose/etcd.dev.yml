services:
  etcd:
    container_name: nexus-etcd-dev
    ports:
      - "${DEV_ETCD_PORT:-12379}:2379"
    volumes:
      - ${NEXUS_RUNTIME_ROOT:-../.runtime}/etcd-dev/data:/etcd-data

networks:
  nexus:
    name: nexus-dev

volumes: {}
