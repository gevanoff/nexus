services:
  ollama:
    container_name: nexus-ollama-dev
    ports:
      - "${DEV_OLLAMA_PORT:-11435}:11434"
    volumes:
      - ../.runtime/ollama-dev:/root/.ollama

networks:
  nexus:
    name: nexus-dev

volumes: {}
