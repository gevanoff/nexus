services:
  # Dev override for the Etcd service.
  # Shared host paths:
  # - ${NEXUS_RUNTIME_ROOT:?NEXUS_RUNTIME_ROOT must be set}/etcd-dev/data is etcd state for dev
  etcd:
    container_name: nexus-etcd-dev
    ports:
      - "${DEV_ETCD_PORT:-12379}:2379"
      - "${DEV_ETCD_PEER_PORT:-12380}:2380"
    volumes:
      - ${NEXUS_RUNTIME_ROOT:?NEXUS_RUNTIME_ROOT must be set}/etcd-dev/data:/etcd-data

networks:
  nexus:
    name: nexus-dev

volumes: {}
