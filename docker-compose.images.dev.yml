services:
  # Dev override for the Images service.
  # Shared host paths:
  # - ${NEXUS_RUNTIME_ROOT:?NEXUS_RUNTIME_ROOT must be set}/images-dev/* is images service state for dev
  images:
    container_name: nexus-images-dev
    ports:
      - "${DEV_IMAGES_PORT:-17860}:7860"
    environment:
      - SHIM_MODE=${DEV_IMAGES_SHIM_MODE:-stub}
      - SHIM_PORT=7860
    volumes:
      - ${NEXUS_RUNTIME_ROOT:?NEXUS_RUNTIME_ROOT must be set}/images-dev/data:/data
      - ${NEXUS_RUNTIME_ROOT:?NEXUS_RUNTIME_ROOT must be set}/images-dev/models:/data/models

networks:
  nexus:
    name: nexus-dev

volumes: {}
