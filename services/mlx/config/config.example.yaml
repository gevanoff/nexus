server:
  host: 0.0.0.0
  port: 10240

models:
  # Primary text/chat model
  - served_model_name: mlx-community/Qwen3-30B-A3B-4bit
    model_path: mlx-community/Qwen3-30B-A3B-4bit
    model_type: lm

  # Dedicated coder model
  - served_model_name: mlx-community/Qwen3-Coder-Next-8bit
    model_path: mlx-community/Qwen3-Coder-Next-8bit
    model_type: lm

  # Dedicated embeddings model
  - served_model_name: mlx-community/bge-small-en-v1.5-8bit
    model_path: mlx-community/bge-small-en-v1.5-8bit
    model_type: embeddings

  # Optional multimodal model for image/audio-aware chat
  # - served_model_name: mlx-community/Qwen2-VL-7B-Instruct-4bit
  #   model_path: mlx-community/Qwen2-VL-7B-Instruct-4bit
  #   model_type: multimodal

  # Add large optional models incrementally. Multi-handler mode loads every
  # configured model during startup, so one slow or incompatible model can keep
  # the whole service from binding its port.

  # Optional lower-latency text tier if you want a second general model on the same host.
  # - served_model_name: mlx-community/Qwen3-4B-8bit
  #   model_path: mlx-community/Qwen3-4B-8bit
  #   model_type: lm

  # Optional image generation model. Uncomment after installing the image extras
  # for mlx-openai-server and selecting a config_name supported by that model.
  # - served_model_name: flux2-klein-4b
  #   model_path: black-forest-labs/FLUX.2-klein-4B
  #   model_type: image-generation
  #   config_name: flux2-klein-4b
  #   quantize: 4

  # Optional image editing model.
  # - served_model_name: qwen-image-edit
  #   model_path: mlx-community/Qwen-Image-Edit-4bit
  #   model_type: image-edit
  #   config_name: qwen-image-edit

  # Optional Whisper transcription model (requires ffmpeg on the host).
  # - served_model_name: mlx-community/whisper-large-v3-mlx
  #   model_path: mlx-community/whisper-large-v3-mlx
  #   model_type: whisper
